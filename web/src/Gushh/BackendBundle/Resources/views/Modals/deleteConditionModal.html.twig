<div class="modal fade modal-fade-in-scale-u" id="deleteForm" aria-hidden="true" aria-labelledby="deleteForm"
role="dialog" tabindex="-1">
  <div class="modal-dialog modal-center">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">×</span>
        </button>
        <h4 class="modal-title">Removing a condition</h4>
      </div>
      <div class="modal-body">
        <p>Are you sure you want to remove the condition?</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        {{ form_start(deleteForm) }}
          {{ form_rest(deleteForm) }}
          <button type="submit" class="btn btn-danger">
            <i class="icon fa-trash" aria-hidden="true"></i> Delete
          </button>
        {{ form_end(deleteForm) }}
      </div>
    </div>
  </div>
</div>